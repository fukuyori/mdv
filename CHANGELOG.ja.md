# 変更履歴

このプロジェクトの主な変更点を記録しています。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に基づき、
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

[English](CHANGELOG.md) | 日本語

## [0.6.5] - 2026-09-01

### 追加

- 現在のプレビュー表示を UTF-8 の Markdown ファイルとして保存する
  「プレビューをエクスポート」を追加。原文・対訳・翻訳表示に対応し、読み取り専用の
  追従表示(`-f`)とセッション(`-s`)モードでも原文をエクスポート可能。

## [0.6.4] - 2026-08-29

### 追加

- GitHub Actions による CI(`.github/workflows/ci.yml`)を追加。
  `qt-default-version.txt` の Qt で Linux Release・Linux ASan/UBSan・macOS・
  Windows(MSVC)をビルドし、ヘッドレス WebEngine 試験を含む CTest 全体を実行。
  スクリプトには ShellCheck と PowerShell 構文チェックを実施。CMake オプション
  `MDV_SANITIZE`(ツリー全体の AddressSanitizer + UndefinedBehaviorSanitizer)と
  `MDV_WEBENGINE_TEST_CHROMIUM_FLAGS`(WebEngine 試験用の Chromium フラグ。
  サンドボックスを使えない CI ランナー向けに `--no-sandbox` を渡せる)を追加。
  アプリケーションターゲットは `-Wconversion -Wsign-conversion` 付きでコンパイル
  するようにし、`main.cpp` に残っていた 4 件の縮小変換を明示的なキャストに修正。

### ドキュメント

- README(日英)に、脅威モデルと実装済みの防御を説明する**セキュリティ**節、
  CTest ターゲットとヘッドレス WebEngine の要件をまとめた**テスト**節を追加し、
  プロジェクト構成を更新。機能一覧から生 HTML の記述を削除し、Windows 署名の
  説明をパスワード付き `.pfx` を拒否する現在の挙動に合わせた。

### セキュリティ

- プレビューがローカル画像・メディアを読み込む範囲を文書のディレクトリ配下に
  限定。`QWebEngineUrlRequestInterceptor`により、それ以外のサブリソース要求
  (他のローカルファイル、`..`による脱出、ディレクトリ外を指すシンボリック
  リンク、`file:`/`data:`以外のスキーム)をネットワーク層に到達する前に遮断し、
  既存のContent Security Policyを補強。判定ロジックは`src/preview_policy.cpp`
  に分離し、新しいCTest `mdv_preview_policy_test`で検証。
- QWebChannelブリッジが引数を検証するように変更。スクロール位置は範囲内の
  有限値のみ受け付け、コピー文字列は1 MiBまでに制限し、ページ側の状態がQt側で
  無制限の処理を引き起こさないようにした。
- 保存時に`QLockFile`(`<ファイル>.mdv-lock`)を外部変更チェック・一時ファイル
  書き込み・renameの間ずっと保持し、`commit()`直前にディスク上のSHA-256を
  再確認するように変更。競合チェックと書き込みの間にダイアログが入らないよう、
  エンコーディング損失の確認を競合チェックより前へ移動。保存中に変更された
  ファイルは黙って上書きせず報告し、別のmdvがロック中の場合も報告する。
- 単一インスタンス用ソケットをさらに堅牢化。Unixではユーザー専用の実行時
  ディレクトリ(`$XDG_RUNTIME_DIR`またはQtのRuntimeLocation。所有者が本人かつ
  0700の場合のみ)に、数値UIDを含む名前で作成し、共有一時ディレクトリでの
  名前の先取りを防止。接続元プロセスのUIDが異なる接続は拒否
  (`SO_PEERCRED` / `getpeereid`)。停止したクライアントは5秒で切断、同時接続は
  最大16、受信パスは絶対かつ正規化済みであることを要求。2回目以降の起動は
  相対パスを自身の作業ディレクトリで解決してから引き渡すようにし、別の
  ディレクトリ基準で開かれる不具合も修正。
- 翻訳要求を送信側でも制限。1ブロックは256 KiB、共有キューは8192件/32 MiBを
  上限とし、超過分はバッファせず失敗ブロックとして報告。翻訳要求とモデル一覧の
  取得はHTTPリダイレクトを追従しないようにし、文書本文が設定で検証(ホスト外の
  平文HTTPは確認)済みのエンドポイント以外へ送られないようにした。3xx応答は
  失敗ブロックとして報告する。
- 新しいCTest `mdv_preview_webengine_test`を追加。プレビューと同じ設定・
  Content Security Policy・リクエストインターセプターを適用した実機
  (オフスクリーン)WebEngineページに攻撃用文書を読み込み、インラインスクリプトと
  イベントハンドラが実行されないこと、文書ディレクトリ内の画像だけが読み込まれる
  こと、`fetch`・`file:`へのXHR・iframe・object・スタイルシート・WebSocket・
  `sendBeacon`がすべて遮断されること、文書が到達を試みるローカルHTTPサーバーに
  接続が届かないことを検証。テストが製品コードを使うよう、インターセプターと
  CSP生成を`src/preview_interceptor.*`と`src/preview_policy.*`へ分離。

## [0.6.3] - 2026-08-29

### 追加

- Antigravity のトランスクリプトログファイル(`~/.gemini/antigravity-cli/brain` 配下に保存される `transcript.jsonl` / `transcript_full.jsonl` 形式)の自動判定および時刻付き会話表示に対応。

- `-s`/`--session` オプションを拡張し、Codex、Claude Code、Antigravity の中から最終更新日時が最も新しい AI セッションを自動検出して追従表示するよう機能向上。

- 新しい`-s`/`--session`オプションを追加。`~/.codex/sessions`以下のログを
  検証し、カレントディレクトリに関係なく最終更新されたCodexセッションを
  追従表示モードで開くため、ログの場所を探さずに進行中のセッションを閲覧可能に。

- 追従表示モードでClaude Codeのセッション`.jsonl`ファイル(`~/.claude/projects`
  以下に保存される形式)も認識し、最近のユーザー・エージェント発言を時刻付きの
  会話として表示。元ログを変更せず、思考ブロック、ツール呼び出しと結果、
  サブエージェントのサイドチェーン、スラッシュコマンドの記録、メタデータ行を
  プレビューから除外し、同じ話者の連続する行は1つのセクションにまとめて表示。

### 修正

- セッション検出時に`session_meta`を16 KiBで切り捨てず、上限付きで先頭行全体を
  読み込むよう修正。エージェント指示を多く含む現在のCodexログも認識し、古い
  セッションへ誤ってフォールバックしないように変更。

## [0.6.2] - 2026-08-29

### 追加

- 追従表示モードでCodexの`rollout-*.jsonl`イベントログを認識し、最近の
  ユーザー・エージェント発言だけを時刻付きの会話として表示。元ログを変更せず、
  重複通知、指示、推論、ツールイベント、コマンド出力をプレビューから除外。

## [0.6.1] - 2026-08-29

### 修正

- 追従表示モードでファイルを開いた時とローテーション後の再読み込み時に、
  プレビューの末尾表示に合わせて目次も最後の見出しが見える位置へスクロール。

## [0.6.0] - 2026-08-29

### 追加

- プレビューで選択した範囲だけを右クリックメニューまたは「翻訳」メニューから
  翻訳し、原文と訳文の確認および訳文のコピーができる機能を追加。通常・
  ビューアー・追従表示の各モードで利用可能。
- 読み取り専用の追従表示モード(`-f`)を追加。保存操作による上書きを防ぎつつ、
  編集ペインを隠してファイルの変更を自動で再読み込みし、最終行を
  プレビューの下端へ表示し続ける。「対訳」「翻訳」表示は無効化する一方、
  選択範囲の翻訳は利用可能。追記部分だけを増分読み込みし、表示範囲を
  末尾10,000行かつ8 MiBに制限して大容量ファイルへ対応。過去の内容へ
  スクロールすると閲覧位置を維持したまま自動スクロールを一時停止し、末尾へ
  戻るか `Esc` キーを押すと最新内容への追従を再開。

### セキュリティ

- Markdown内の生HTMLをWebEngineのDOMへ挿入せず、エスケープして表示するように
  変更。プレビューへCSPを設定し、外部リソースの自動取得、JavaScriptからの
  クリップボード利用、LocalStorageも無効化。
- Highlight.jsを11.11.1から11.12.0へ更新し、XML文法のReDoSを修正。
  プレビューの可用性を守る一般的な上限として、100,000文字を超えるコード
  ブロックは引き続きシンタックスハイライトの対象外。
- 同梱MD4Cを0.5.2から0.5.3へ更新し、悪意あるリンク参照の展開、二次時間の
  行検索、安全でないエラー経路などを修正。
- 単一インスタンスIPCをユーザー限定権限と安定したユーザー別名称に変更し、
  1 MiBのメッセージ上限およびペイロード・パス検証を追加。
- 翻訳ジョブが受付時のエンドポイント・モデル・言語を保持するようにし、応答
  サイズ上限、URL検証、暗号化されていないリモート送信先への確認を追加。
- 保存前に読み込み時のSHA-256とディスク上のファイルを比較し、`assets`が
  シンボリックリンクの場合は画像の貼り付けを拒否するように変更。
- 通常モードの上限を256 MiBとし、それを超えるファイルは上限付きの追従モードを
  案内。配布処理の再帰削除先をリポジトリの`dist`配下に限定し、Windows署名では
  プロセスのコマンドラインへ露出する証明書パスワードを受け付けないように変更。

### 変更

- コンパイラの防御オプションと、生HTML・病的なリンク参照展開に対する
  セキュリティ回帰テストを追加。
- 対応Qtの下限を6.10.3へ更新し、Qt 6.11.2でReleaseビルドを検証。
  Linux・macOSのReleaseビルドスクリプトへ`QT_ROOT`によるQt選択を追加。
- ReleaseビルドとAppImage作成でQt 6.11.2を既定選択し、古いシステムQtへ
  黙ってフォールバックしないように変更。各OSでQtパスの明示指定は引き続き可能。
  全スクリプトが`qt-default-version.txt`から共通の既定バージョンを読み込む。

## [0.5.3] - 2026-08-06

### 追加

- 同梱の Highlight.js でフェンスコードブロックをシンタックスハイライトし、
  GitHub 形式の5種類のアラートを描画できるようにした。

## [0.5.2] - 2026-08-06

### 追加

- Mermaid のフェンスコードブロックと、インライン・ブロック形式の LaTeX 数式を
  同梱の Mermaid・KaTeX ライブラリでオフライン描画できるようにした。

### 変更

- 起動時の無題ドキュメントから組み込みの mdv 説明文を削除し、
  空の内容で表示するように変更。

## [0.5.1] - 2026-07-30

### 修正

- 左側の目次で見出しをクリックすると、エディタペインを非表示にしている場合も
  プレビューの対応する位置へ移動するように修正。
- Linux で GUI アプリケーションの生成前にアプリ情報を初期化し、
  host portal のアプリ ID が重複登録される警告を解消。

## [0.5.0] - 2026-07-28

### 追加

- プレビューのコンテキストメニューから、選択した表示テキストをコピー可能にした。
- プレビューの各コードブロックに、ブロック全体をコピーするアイコンボタンを
  追加。コピー後は一時的にチェックマークを表示する。
- Ctrl+Q で、開いているすべての mdv ウインドウを含めてアプリを終了できるようにした。

### 修正

- ライト・セピア・ダークの各テーマで、翻訳設定のモデルおよび翻訳先言語の
  ドロップダウン候補を読み取れる配色に修正。

## [0.4.0] - 2026-07-11

### 追加

- エディタに Markdown 向けの入力補助を追加:
  - 改行時に前の行のインデント(スペース/タブ)を引き継ぐ。
  - 箇条書き(`-` `*` `+`)、番号付きリスト(`1.` / `1)` は次番号)、
    チェックボックス(`- [ ]`)、引用(`>`)のマーカーを次の行に自動挿入。
    マーカーだけの行で Enter を押すとマーカーを消してリストを終了。
  - Shift+Enter はマーカー補完なしのプレーン改行(インデントのみ維持)。
  - 複数行選択時の Tab / Shift+Tab で選択行をまとめてインデント/解除
    (Shift+Tab は選択なしの単一行でも可)。

## [0.3.2] - 2026-07-09

### 追加

- プレビュー横に編集ペインの表示/非表示を切り替える細いトグルを追加。

### 修正

- タブ作成直後からプレビュー表示切り替えボタンのラベルが表示されるように修正。

## [0.3.1] - 2026-07-08

### 追加

- 複数ウインドウ対応: タブを右クリックして「新しいウインドウで開く」を
  選ぶと、そのタブだけを新しいウインドウへ移動できる(タブが2つ以上ある
  ときのみ表示)。
- タブをタブバーの外へドラッグすると、そのタブを新しいウインドウとして
  切り離せる。ドラッグ中はタブの半透明のプレビューがカーソルに追従する。
- タブをドラッグして別の mdv ウインドウへドロップすると、新しいウインドウ
  を作らずそちらへタブを移動する。移動元のウインドウの最後の1タブを
  移動した場合は、そのウインドウは閉じてウインドウが1つだけになる。

## [0.3.0] - 2026-07-08

### 追加

- タブ機能: 複数の Markdown ドキュメントを同時に開いて編集可能に。
  各タブは独立したエディタ・アウトライン・プレビューを持つ。タブの
  新規作成・閉じる(未保存の変更がある場合は確認ダイアログを表示)・
  Ctrl+Tab / Ctrl+Shift+Tab での切り替えに対応。
- すでに開いているファイルを再度開こうとした場合、重複したタブを
  作らずに既存のタブへ切り替えるように。
- コマンドライン引数で複数ファイルを指定した場合、およびウィンドウへ
  複数ファイルをドラッグ&ドロップした場合(ドラッグ&ドロップでの
  ファイルオープンは新機能)、それぞれ別タブとして開くように対応。
- 翻訳はバックグラウンドのタブでも継続。あるタブを原文表示に戻しても、
  同じ内容を翻訳中の他のタブには影響しない。
- タブが開いているファイルがディスク上で変更された(他のプログラムに
  よる編集など)ことを検知し、再読み込みするか確認するように。表示中の
  タブは即座に、バックグラウンドのタブは選択した時点で確認する。未保存の
  編集がある状態で再読み込みを見送った場合、終了時の保存確認ダイアログで
  「保存」が外部の変更を上書きし「破棄」が外部の変更を残すことを明示する
  ように変更。

## [0.2.2] - 2026-07-08

### 修正

- macOS: プレビューペインが常に真っ白になり、アクセシビリティクライアント
  (VoiceOver や Karabiner など)がウィンドウを照会すると起動直後に
  クラッシュすることがある問題を修正。署名済みアプリに同梱の
  QtWebEngineProcess ヘルパーが Homebrew の Qt ライブラリを絶対パスで
  参照したままだったため、Chromium レンダラプロセスが起動できなかった。
  macOS パッケージングスクリプトがバンドル内の全ライブラリ参照を
  @rpath に書き換え、不足していた画像フォーマット依存ライブラリ
  (libjpeg・libwebp)を同梱するように変更。

## [0.2.1] - 2026-07-07

### 追加

- `--version` コマンドラインオプションでバージョンを標準出力に表示。
- ステータスバー右側にバージョン番号を常時表示。

## [0.2.0] - 2026-07-06

### 追加

- ローカルの [Ollama](https://ollama.com) サーバーによるプレビューの翻訳機能。
  プレビュー上部のボタンまたは新設の「翻訳」メニューから、
  「原文」「対訳」(原文と訳文を交互に表示)「翻訳」の3表示を切り替え可能。
- 翻訳の設定ダイアログ: Ollama エンドポイント、モデル(サーバーから
  インストール済み一覧を取得)、翻訳先言語(日本語/English)、
  同時リクエスト数(1〜8)。
- ブロック単位の翻訳と逐次表示: 訳文は到着したブロックから順次表示され、
  ブロック単位でキャッシュされるため、編集時は変更したブロックだけが
  再翻訳されます。
- 表示位置優先の翻訳順: 現在表示中の位置から下のブロックを読み順で優先し、
  スクロールに合わせて待機キューを並べ替えます。
- ブロック単位のエラー処理: 翻訳に失敗したブロック(サーバーエラー、
  タイムアウト、空応答)は対訳表示で「(翻訳失敗)」マーカー付きの原文表示と
  なり、残りの翻訳は継続します。全体を中止するのはサーバーに接続できない
  場合のみです。
- Linux 向けリリースビルド・パッケージングスクリプト(deb / tarball)。
- Linux でのアプリケーションアイコンの埋め込み。

### 修正

- Linux でフォントダイアログが文字化けする問題。
- macOS リリース署名スクリプトの修正。

## [0.1.2] - 2026-07-05

### 追加

- Windows 向けビルド・パッケージングスクリプト(Inno Setup インストーラ)と
  Windows アプリケーションアイコン。

## [0.1.1] - 2026-07-05

### 追加

- macOS からのファイル受け渡しに対応: `open -a`、Finder の「このアプリケーションで開く」、
  Dock アイコンへのドロップ。

## [0.1.0] - 2026-07-05

### 追加

- 初回リリース: C++ と Qt Widgets 製の Markdown ビュワー/エディタ。
  GitHub Flavored Markdown のライブプレビュー(Qt WebEngine + md4c)、
  双方向スクロール同期、見出しアウトライン、プレビューハイライト付きの
  検索と置換、文字コードの安全性チェック、最近開いたファイル、
  クリップボード画像の貼り付け、ライト/ダーク/セピアテーマ、フォント選択、
  日英 UI、編集ペインを隠すビュワーモード。
